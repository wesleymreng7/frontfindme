import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles(theme => ({
  container: {
    margin: '0 auto',
    width: '800px',
    marginTop: '230px'
  },
  buttonGo: {
      marginTop: '50px'
  }
}))

export default useStyles
